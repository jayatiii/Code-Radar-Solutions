#include <stdio.h>

char* welcome() {
    return "Welcome to Code Radar!";
}

int main() {
    string a;
    string b;
    scanf("%s %s", &a , &b);
    print("You entered: %s and \n %s", a , b);
    return 0;
}