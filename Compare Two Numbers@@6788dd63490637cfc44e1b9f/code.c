#include <stdio.h>

char* welcome() {
    return "Welcome to Code Radar!";
}

int main() {
    int a;
    scanf("%d %d", &a, &b );
    if ( a > b) {
        printf("First");
    }
    if ( a < b) {
        printf("First");
    }
    else{
        printf("Equal");
    }
    return 0;
}